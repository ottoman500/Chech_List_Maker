export type CheckList = {
    check_list_id: number,
    check_list_name: string
}

export type SelectedCheckList = {
    selected_check_list_id: number,
    check_list_name: string,
    check_list_info: string[]
}