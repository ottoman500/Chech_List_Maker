# 画面構成
1. チェックリスト選択画面
2. チェックリスト画面
3. チェックリスト作成編集画面

# 画面遷移図
```plantuml
@startuml
rectangle "チェックリスト選択画面" as Select
rectangle "チェックリスト画面" as Checklist
rectangle "チェックリスト作成編集画面" as Edit_Create

Select --> Checklist: チェックリスト選択
Select --> Edit_Create: 新規作成

Checklist --> Edit_Create: 編集
Checklist --> Select: 戻る

Edit_Create --> Checklist: 戻る（編集時）
Edit_Create --> Select: 戻る（新規作成時）
@enduml

```

# シーケンス
## 一覧取得
```plantuml
@startuml
actor User
participant "web ui" as UI
participant "API" as API
database "DB" as DB

User -> UI: ログインまたはページ変化
UI -> API: ページ数を送る
API -> DB: チェックリストの一覧取得
DB --> API: チェックリストの一覧データ
API --> UI:チェックリストの一覧データ
UI --> User:チェックリストの一覧表示
@enduml
```

## チェックリスト表示時
```plantuml
@startuml
actor User
participant "web ui" as UI
participant "API" as API
participant "DB" as DB

User -> UI:チェックリストの選択
UI -> API:指定されたチェックリストの取得
API -> DB:指定されたチェックリストの取得
alt 取得の成功
    DB --> API:チェックリストのデータ
    API --> UI:チェックリストのデータ
    UI --> User:チェックリストの表示
else 取得の失敗
    DB --> API:失敗
    API --> UI:エラーメッセージ
    UI --> User:エラーメッセージの表示
end

@enduml
```

## 追加・編集時
```plantuml
@startuml
actor User
participant "web ui" as UI
participant "api" as API
participant "db" as DB

User -> UI: チェックリストの情報を入力する
UI -> API:チェックリストの情報を送る
API -> DB:チェックリストの情報を登録する
alt 登録に成功したとき
    DB --> API:成功
    API --> UI:成功のメッセージを送る
    UI --> User:成功のメッセージを表示する
else 登録に失敗したとき
    DB --> API:失敗
    API --> UI:失敗のメッセージを送る
    UI --> User:失敗のメッセージを表示する
end
@enduml
```

## 削除
```plantuml
@startuml
actor User
participant "web ui" as UI
participant "API" as API
participant "DB" as DB

User -> UI:チェックリストを選択
UI -> API:選択されたチェックリストを削除
API -> DB:チェックリストの削除
alt 削除に成功したとき
    DB --> API:成功
    API --> UI:メッセージ
    UI --> User:成功のメッセージ表示
else 削除に失敗したとき
    DB --> API:失敗
    API --> UI:メッセージ
    UI --> User:失敗のメッセージ表示
end
@enduml
```

# API設計
## チェックリスト一覧取得
### リクエスト
* HTTPメソッド：GET
* URL：/List
```
{
    "page":"1",
}
```
### レスポンス
* ステータス：200
```
{
    "list":[
        {"selected_check_list_id":"1", "check_list_name":"name"},
        {"selected_check_list_id":"2", "check_list_name":"name"},
        {"selected_check_list_id":"3", "check_list_name":"name"},
    ]
}
```
## チェックリスト取得
* HTTPメソッド：GET
* URL：/SelectedCheckList
```
{
    "selected_check_list_id":"1",
    "check_list_name":"name",
    "check_list_info":[
        {"key_1":"項目1"},
        {"key_2":"項目2"},
        {"key_3":"項目3"},
    ]
}
```

## 追加・編集
* HTTPメソッド：PUT
* URL：/NewCheckList
```
{
    "selected_check_list_id":"1", // 新規追加の時は空文字にする
    "check_list_name":"name",
    "check_list_info":[
        {"key_1":"項目1"},
        {"key_2":"項目2"},
        {"key_3":"項目3"},
    ]
    "status":"0" // 0なら新規追加で1なら編集
}
```

## 削除
* HTTPメソッド：DELETE
* url：/UnnecessaryCheckList
```
{
    "unnecessary_check_list_id":"1"
}
```

# データ構造
* DB名：check_list_table
* カラム：
  * id serial primary
  * check_list_name text not null
  * check_list text not null
**具体的なチェックリストはjson文字列で持つようにする**